--'let abc = 5 \* 5' -> is the input string to which we convert it into tokens as set defined and we call is lexer

-- LET IDENT EQ INT MULTIPLY INT -> This is the Tokenized Form of the input string that gets into the lexer

-- In the parser, we iterate through the Tokenized output of the input sting make AST

-- So you Iterate through the tokens as

-- for loop into tokens -> Parse Sentences --> Contains many parsing switch cases like let,fn etc --> goes over to each of their parsing functions
